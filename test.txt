#!/bin/bash

echo "🚀 TEST COMPLET DU PROFIL UTILISATEUR"
echo "====================================="

# 1. Connexion
echo -e "\n1. 🔐 Connexion"
LOGIN_RESPONSE=$(curl -s -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"sissolionel@gmail.com","password":"Secret123!"}'***REMOVED***

echo "Réponse: $(echo $LOGIN_RESPONSE | head -c 150***REMOVED***..."

# Extraire les tokens
ACCESS_TOKEN=$(echo $LOGIN_RESPONSE | python3 -c "
***REMOVED***, json
try:
    data = json.load(sys.stdin***REMOVED***
    print(data.get('accessToken', ''***REMOVED******REMOVED***
except:
    print(''***REMOVED***
" 2>/dev/null***REMOVED***

if [ -z "$ACCESS_TOKEN" ]; then
    echo "❌ Impossible d'obtenir le token d'accès"
    exit 1
fi

echo "✅ Token obtenu"

# 2. Récupérer le profil
echo -e "\n2. 👤 Récupérer le profil"
PROFILE_RESPONSE=$(curl -s -X GET http://localhost:3000/api/profile \
  -H "Authorization: Bearer $ACCESS_TOKEN"***REMOVED***

echo "Profil:"
echo $PROFILE_RESPONSE | python3 -m json.tool 2>/dev/null || echo $PROFILE_RESPONSE

# 3. Mettre à jour le profil
echo -e "\n3. ✏️ Mettre à jour le profil (changer le prénom***REMOVED***"
UPDATE_RESPONSE=$(curl -s -X PUT http://localhost:3000/api/profile \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "Lionel_Updated",
    "lastName": "Sisso_Updated"
  }'***REMOVED***

echo "Mise à jour:"
echo $UPDATE_RESPONSE | python3 -m json.tool 2>/dev/null || echo $UPDATE_RESPONSE

# 4. Vérifier la mise à jour
echo -e "\n4. 🔍 Vérifier le profil mis à jour"
UPDATED_PROFILE=$(curl -s -X GET http://localhost:3000/api/profile \
  -H "Authorization: Bearer $ACCESS_TOKEN"***REMOVED***

echo "Profil mis à jour:"
echo $UPDATED_PROFILE | python3 -c "
***REMOVED***, json
try:
    data = json.load(sys.stdin***REMOVED***
    profile = data.get('profile', {}***REMOVED***
    print(f'Prénom: {profile.get(\"firstName\", \"N/A\"***REMOVED***}'***REMOVED***
    print(f'Nom: {profile.get(\"lastName\", \"N/A\"***REMOVED***}'***REMOVED***
    print(f'Email: {profile.get(\"email\", \"N/A\"***REMOVED***}'***REMOVED***
    print(f'Vérifié: {\"Oui\" if profile.get(\"emailVerifiedAt\"***REMOVED*** else \"Non\"}'***REMOVED***
except Exception as e:
    print(f'Erreur: {e}'***REMOVED***
" 2>/dev/null

# 5. Tester la validation pour changement d'email
echo -e "\n5. 📧 Tenter de changer l'email sans mot de passe (devrait échouer***REMOVED***"
EMAIL_FAIL=$(curl -s -X PUT http://localhost:3000/api/profile \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "newemail@example.com"
  }'***REMOVED***

echo "Tentative changement email sans mot de passe:"
echo $EMAIL_FAIL | python3 -c "
***REMOVED***, json
try:
    data = json.load(sys.stdin***REMOVED***
    if data.get('success'***REMOVED*** == False:
        print('✅ Échec attendu:', data.get('error', 'Erreur inconnue'***REMOVED******REMOVED***
    else:
        print('❌ Devrait avoir échoué mais a réussi'***REMOVED***
except:
    print('Réponse brute:', data***REMOVED***
" 2>/dev/null

# 6. Tenter de changer l'email avec mauvais mot de passe
echo -e "\n6. 🔒 Tenter de changer l'email avec mauvais mot de passe"
EMAIL_WRONG_PASS=$(curl -s -X PUT http://localhost:3000/api/profile \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "newemail@example.com",
    "currentPassword": "WRONG_PASSWORD"
  }'***REMOVED***

echo "Tentative avec mauvais mot de passe:"
echo $EMAIL_WRONG_PASS | python3 -c "
***REMOVED***, json
try:
    data = json.load(sys.stdin***REMOVED***
    if data.get('success'***REMOVED*** == False:
        print('✅ Échec attendu:', data.get('error', 'Erreur inconnue'***REMOVED******REMOVED***
    else:
        print('❌ Devrait avoir échoué mais a réussi'***REMOVED***
except:
    print('Réponse brute:', data***REMOVED***
" 2>/dev/null

# 7. Test de suppression de compte (avec mauvais mot de passe***REMOVED***
echo -e "\n7. 🗑️ Tester la suppression de compte (avec mauvais mot de passe - doit échouer***REMOVED***"
DELETE_FAIL=$(curl -s -X DELETE http://localhost:3000/api/profile \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "password": "WRONG_PASSWORD",
    "confirm": "WRONG_PASSWORD"
  }'***REMOVED***

echo "Tentative suppression avec mauvais mot de passe:"
echo $DELETE_FAIL | python3 -c "
***REMOVED***, json
try:
    data = json.load(sys.stdin***REMOVED***
    if data.get('success'***REMOVED*** == False:
        print('✅ Échec attendu:', data.get('error', 'Erreur inconnue'***REMOVED******REMOVED***
    else:
        print('❌ Devrait avoir échoué mais a réussi'***REMOVED***
except:
    print('Réponse brute:', data***REMOVED***
" 2>/dev/null

# 8. Logout
echo -e "\n8. 👋 Déconnexion"
# D'abord, rafraîchir pour obtenir un refresh token valide
REFRESH_TOKEN=$(echo $LOGIN_RESPONSE | python3 -c "
***REMOVED***, json
try:
    data = json.load(sys.stdin***REMOVED***
    print(data.get('refreshToken', ''***REMOVED******REMOVED***
except:
    print(''***REMOVED***
" 2>/dev/null***REMOVED***

LOGOUT_RESPONSE=$(curl -s -X POST http://localhost:3000/api/auth/logout \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"refreshToken\":\"$REFRESH_TOKEN\"}"***REMOVED***

echo "Déconnexion: $LOGOUT_RESPONSE"

# 9. Vérifier que le token est bien révoqué
echo -e "\n9. 🔍 Vérifier que le token est révoqué"
REVOKED_CHECK=$(curl -s -X GET http://localhost:3000/api/profile \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  -w " HTTP Status: %{http_code}"***REMOVED***

echo "Accès avec token révoqué: $REVOKED_CHECK"

echo -e "\n✅ TEST COMPLET TERMINÉ"
echo "====================================="